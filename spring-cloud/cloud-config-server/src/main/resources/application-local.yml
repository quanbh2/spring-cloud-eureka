server:
  port: 5000

spring:
  profiles:
    include: native
  cloud:
    config:
      server:
        native:
          search-locations: classpath:config/
          #search-locations: file:./spring-cloud/cloud-config-server/files-config-repo/